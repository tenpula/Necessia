/*
 * 【ファイル概要】
 * 認証APIのメイン部分（Auth.js）
 * ログインやログアウトなど、認証に関するすべての窓口です。
 */

// =============================================================================
// Auth.js API Route（キャッチオールルート）
// =============================================================================
//
// ■ このファイルの役割:
//   Auth.js が自動的に以下のエンドポイントを処理します:
//   - GET/POST /api/auth/signin    → ログインページ
//   - GET/POST /api/auth/signout   → ログアウト処理
//   - GET/POST /api/auth/callback  → OAuthコールバック（Google認証後のリダイレクト先）
//   - GET      /api/auth/session   → 現在のセッション取得
//   - GET      /api/auth/providers → 利用可能な認証プロバイダー一覧
//
// ■ [...nextauth] とは？
//   Next.js の「キャッチオールルート」構文。
//   /api/auth/* にマッチする全てのリクエストをこのファイルで処理します。
// =============================================================================

import { handlers } from '@/lib/auth';

export const { GET, POST } = handlers;
